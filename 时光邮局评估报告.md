# 时光邮局（Time Post Office）评估报告

> **评估方**：Cursor Agent  
> **对照基准**：《时光邮局功能提案.md》v1.0 + senior-post 当前代码（Flutter 四 Tab、Spring Boot 信箱/邮票/IM/墙）  
> **用途**：与其他 AI / 产品评审「碰撞」需求，非实施文档、不修改项目内其他 doc

---

## 1. 一句话结论

**时光邮局值得做，但不能当成「现有平邮的加长版」**——应作为 **独立业务线**（新表、新入口、新列表），复用邮票/OSS/好友/调度模式；提案里 **情感叙事与邮票闭环** 很好，**Push、分享墙奖票、独立时光邮票、24 人日工期** 等应 v1 砍掉或后置。

---

## 2. 结合当前项目：好在哪里（建议保留的方向）

| # | 提案亮点 | 与现项目的契合 |
|---|----------|----------------|
| G1 | **邮政 + 慢时间** 的心智 | 已有平邮/挂号、齿孔 UI、`Postal*` 组件；用户已理解「信不是立刻到」 |
| G2 | **写给未来的自己** | 现有信箱全是 **双人社交信**（名录→发信→Accept→IM），缺「自我记录」场景，补位清晰 |
| G3 | **写给亲友** | `bu_friendship` + Connections 已存在，校验「是否笔友」有现成 API 思路 |
| G4 | **延迟送达** | `StandardLetterDeliveryService` 已证明：**PG 扫 `deliver_at` + 乐观更新** 在本项目可行（不必先上 Redis） |
| G5 | **邮票消耗** | `stamps_balance` + `log_stamp_transaction` 已有；新 reason 枚举即可，增加消耗场景 |
| G6 | **附图** | OSS put-sign + 机审管线（明信片）可借鉴，降低从零成本 |
| G7 | **老年友好交互** | 提案里「快捷档 + 大日期选择」与现有 `birth_year_picker`、大触控规范一致 |
| G8 | **差异化** | 墙/名录/IM 偏即时；时光信占 **「仪式感 + 长周期」**，竞品难直接抄品牌 |

---

## 3. 结合当前项目：差在哪里（建议 v1 去掉或改写）

| # | 提案内容 | 问题 | 建议 |
|---|----------|------|------|
| B1 | 复用/混用现有 `bu_letter` 与 Archive | 现有信绑 Accept、postal/sync、10–120 分钟平邮；混用会 **搞乱信箱 Tab** | **新表 + 新 API**，与社交信并行 |
| B2 | 示例 `TimeLetter` + JPA + String id | 项目是 **MyBatis-Plus + Long + Flyway + client/biz 分层** | 仅作概念，实施需按仓库规范重写 |
| B3 | 「时光邮票」独立币种 | 库表只有 **单一 `stamps_balance`** | v1 用普通邮票 + `sys_config` 扣费数 |
| B4 | 写信用 2 枚 + 加急 +1 + 加密 +1 | 扣费规则未配置化；规则叠多 **运营难调** | v1 **单一扣费**；加急/加密 v2 |
| B5 | 读信 +1、分享墙 +2 奖票 | 发帖赠票已有日 cap；易 **刷票** | v1 不奖票；若要奖票需幂等 + 上限设计 |
| B6 | 分享到明信片墙 | 私密信变公开 → **审核 + 二次授权 + 脱敏** | v1 不做；或 v2 单独 PRD |
| B7 | 7/3/1 天 Push 提醒 | Flutter **无 FCM/APNs**；信件也无 Push | v1 **应用内列表/角标**；Push 单独立项 |
| B8 | 送达走「腾讯 IM 消息推送」 | IM 适合好友闲聊；**写给自己** 无对端会话 | 送达以 **App 内 inbox** 为主，IM 仅可选「好友信摘要」 |
| B9 | 死信队列 + 分布式锁 | 现调度 **无 SchedulerLock**；平邮靠条件更新 | v1 复制乐观更新即可；多实例锁 **有部署再加** |
| B10 | 时光档案 / 记忆标签 / 统计大盘 | 属 **v2 内容运营** | v1 用「发件箱/收件箱列表」代替 |
| B11 | 工期 ~24 人日 | 未含 Manage 审核、Flyway、l10n、联调、边界测试 | 现实 v1 MVP 倾向 **4–6 周** |
| B12 | 加密信 + 端到端表述 | 账号有 **7 日注销**；密钥/忘记密码/删号后数据未定义 | v1 不做；要做需安全专项 |
| B13 | 入口未定义 | 四 Tab 已满（墙/名录/信箱/我的） | 需 UX 定：**信箱第三分段** 或 **Profile 入口** |

---

## 4. 发散讨论（供多 AI 碰撞）

### 4.1 产品形态

- **「Future Me」日记线**：只允许写给自己，弱化社交，做成老年版「时间胶囊日记」，好友信 v2 再加——**降低 v1 好友/IM 耦合**。
- **「纪念日信使」**：与系统日历/生日（若未来有）联动，模板「生日那天打开」——比自由选日期 **更易理解**。
- **「家族树投递」**：给子女/孙辈预设多封（提案 v2 联名信简化版）——需好友关系 +  possibly 监护人概念，复杂度高。
- **「未读惊喜」**：发件人在投递前 **不可预览已发送内容**（防后悔改口） vs 允许撤回编辑——产品伦理与客诉风险。

### 4.2 与现有功能的关系

- **平邮 10–120 分钟** = 社交「等一等」；**时光信** = 「真的很久」——UI 文案必须 **强制区分**，避免用户以为挂号能定一年。
- **Connections 即时聊天** vs **时光信**：聊天像电话，时光信像 **定时礼物**；是否在送达日自动生成一条 IM「您有一封时光信」？
- **邮票经济**：时光信是否 VIP 免扣？与挂号免扣规则 **统一还是独立**？
- **注销/删号**：待发信自动取消并退票？已送达未读是否保留给收信人？

### 4.3 技术与运营

- **调度粒度**：每小时 cron vs 30s fixedDelay（现平邮）——对「日级」投递，**每小时足够**。
- **时区**：`deliver_at` 按 UTC 还是用户国家？跨国笔友 **必踩坑**。
- **内容安全**：仅文字 vs 单图 vs 多图——审核成本阶梯式上升。
- **Manage 后台**：待投递列表、强制取消、审计——**老年产品** 仍需客服介入能力。
- **冷启动**：第一个版本无历史信，是否提供 **「写第一封给 3 个月后的自己」引导**？

### 4.4 可探索的「小而美」v1

- 只做 **3 个快捷档**：1 个月 / 6 个月 / 1 年 + 自定义日期  
- 只做 **纯文本**（零机审压力）  
- 只在 **「我的」或信箱内卡片** 入口，不新增 Tab  
- 送达后 **一封信只能读一次** vs 无限重读——影响「档案感」

---

## 5. Cursor 核心需求讨论表（供与其他 AI 对齐）

> 说明：**采纳** = 建议纳入 v1 MVP；**不采纳** = 建议 v1 不做或合并；**待定** = 需产品/UX 拍板。  
> 其他 AI 可增删行、改「是否采纳」列，形成交叉评审。

### 5.1 Cursor 核心需求

| 需求点 | 是否采纳 | 采纳 / 不采纳原因 |
|--------|----------|-------------------|
| 独立「时光邮局」产品模块（与现有双人社交信区分） | **采纳** | 现 `bu_letter` 绑 Accept/postal/archive/短平邮，混用必乱；独立域模型清晰 |
| 写给自己（sender = recipient） | **采纳** | 填补现网空白；无 IM/建联依赖，v1 可先上 |
| 写给 Connections 邮政好友 | **采纳** | `bu_friendship` 已有；与「代际/亲友」叙事一致；需校验 active friendship |
| 写给陌生人 / 名录非好友 | **不采纳** | 与现有「先写信再 Accept」路径冲突；spam 风险；无提案必需 |
| 用户自选未来投递日（≥ 次日） | **采纳** | 核心 TP；与平邮随机分钟本质不同 |
| 快捷档：1 月 / 6 月 / 1 年 + 自定义日期 | **采纳** | 提案方向对；比纯 DatePicker 适老 |
| 最长投递 5 年 | **待定** | 提案有；涉及删号留存、调度数据量；v1 可先 cap **2 年** 验证 |
| 正文 + 敏感词校验 | **采纳** | 复用 `SensitiveWordService`，成本低 |
| 正文 + 多图附件 | **不采纳（v1）** | 机审 + Manage 工作量显著；v1 纯文本或 **单图** 更合适 |
| 新建 `bu_time_letter`（或等价表），不扩 `bu_letter` | **采纳** | 见 B1；Flyway 增量即可 |
| 状态：待投递 → 已送达 → 已读（+ 已取消） | **采纳** | 最小闭环；对齐提案 PENDING/DELIVERED/READ |
| 投递前取消（发件人） | **采纳** | 提案有；需定义 **是否全额退票**（待定细则） |
| 投递前编辑信件 | **待定** | 提案未写；仅取消重写 vs 原地编辑，影响扣票与审计 |
| PG 定时扫表送达（复用平邮调度模式） | **采纳** | 现网已跑通；无需 v1 上 Redis/死信队列 |
| 分布式锁 / 死信队列 | **不采纳（v1）** | 现调度无锁靠乐观更新；实例少时可后置 |
| 发件箱 / 收件箱分页列表 | **采纳** | 提案 inbox/outbox；v1 替代「时光档案」 |
| 已读时间线 / 记忆标签 / 写信统计 | **不采纳（v1）** | 属 v2 运营与回顾；列表足够验证需求 |
| 创建时扣普通邮票（单一 configurable 数量） | **采纳** | 无独立「时光邮票」币种；走 `stamps_balance` + 流水 reason |
| 提案：基础 2 枚 + 加急 +1 + 加密 +1 | **不采纳（v1）** | 规则过细且现网扣费未全配置化；v1 **一口价** |
| 独立「时光邮票」类型 | **不采纳** | 现表结构不支持；增加经济系统复杂度 |
| 读信奖励邮票 | **不采纳（v1）** | 刷票风险；与发帖赠票策略需统一设计 |
| 分享到明信片墙 + 分享奖票 | **不采纳（v1）** | 隐私 + 机审 + 反作弊；见 B6 |
| 密码保护 / 加密信 | **不采纳（v1）** | 注销/忘密/密钥管理未定义；安全专项 |
| 送达：App 内收件列表 + 未读态 | **采纳** | 无 Push 基建下的最小可行通知 |
| 送达：7/3/1 天 Push 提醒 | **不采纳（v1）** | 无 FCM/APNs；单独立项 |
| 送达：腾讯 IM 消息通知好友 | **待定** | 好友信可增强触达；写给自己不适用；需防 IM 骚扰 |
| 加急投递（提前送达） | **不采纳（v1）** | 与「时光」概念略冲突；v2 若要做需单独定价 |
| 独立 App Tab「时光邮局」 | **不采纳（v1）** | 四 Tab 已满；**信箱子入口或「我的」** 成本更低 |
| 信箱 Tab 内第三分段（Postal / Connections / 时光） | **待定** | Cursor 倾向此方案；需 UX 确认是否挤 |
| Manage 端：时光信列表 / 审核 / 强制取消 | **采纳（简版）** | 有图则必审；至少客服可查可拦 |
| API 路径 `/api/time-letters/*`（REST + 分页） | **采纳** | 对齐现有 `/api/mailbox/*` 风格；提案 `/api/time-letter` 单复数可统一 |
| 提案工作量 24 人日 | **不采纳** | 低估前后端+Manage+联调；v1 MVP 宜按 **4–6 周** 量级排期 |
| v2：联名信 / 礼物包 / 电子印章 | **不采纳（v1）** | 提案中期扩展，不进入 v1 讨论表执行 |
| v3：实体明信片 / AI 写信 / 家族时光线 | **不采纳（v1）** | 长期愿景，仅作路线图参考 |

### 5.2 Cursor 建议的 v1 最小需求包（由上表「采纳」项归纳）

1. 新表 + 新 API：`创建 / 取消 / 发件箱 / 收件箱 / 详情 / 标记已读`  
2. 收信人：**自己 + 活跃好友**  
3. 时间：**快捷档 + 日期**，建议 cap 2 年（5 年待定）  
4. 内容：**纯文本**（或至多单图，若团队愿意承担审核）  
5. 邮票：**创建时扣一次**，取消是否退票待定  
6. 调度：**扫表送达**，应用内可见  
7. 入口：**信箱或我的二级入口**（Tab 方案待定）  
8. Manage：**只读列表 + 必要时下架**（若有图则审核）

### 5.3 留给其他 AI 重点「碰撞」的 5 个问题

1. v1 是否 **只做写给自己**，好友信延后？  
2. 入口放 **信箱第三分段** 还是 **Profile**？  
3. 取消待发信是否 **100% 退票**？  
4. 送达通知 **仅 App 内** 还是 **必须上 IM 摘要**？  
5. v1 是否允许 **一张图**（复用 postcard 机审）还是 **纯文字**？

---

*评估报告版本：Cursor v0.1 · 2026-05-25 · 仅用于需求讨论，不绑定项目 doc 治理*
