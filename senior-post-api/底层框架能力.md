# Senior Post 项目框架使用指南

> **文档元信息**  
> **版本**：1.1 · **更新**：2026-05-09 · **维护人**：AI + Owner（框架升级时同步修订）

## 文档目的

本文档旨在帮助 AI 或开发者在 **senior-post** 项目中正确使用框架提供的公共组件，避免重复造轮子。所有框架能力均来自 `commons-`* 模块。

---

## 一、框架能力总览

### 1.1 可用模块


| 模块                               | 提供的功能                                 |
| -------------------------------- | ------------------------------------- |
| **commons-basic**                | 请求上下文、Token解析、异常定义、工具类                |
| **commons-data**                 | 领域模型基类、DTO基类、分页组件                     |
| **commons-web**                  | 统一响应封装、Token认证过滤器、异常处理、优雅停机、Locale拦截器 |
| **commons-security**             | AES数据加解密（APP接口安全）                     |
| **commons-feign-bridge-core**    | 代码生成模板（MyBatis/MVC/Feign）             |
| **commons-feign-bridge-mybatis** | 跨服务调用CRUD接口基类实现                       |
| **commons-redis-starter**        | Redis操作模板（自动配置）                       |


---

## 二、请求上下文 (MyRequestContextHolder)

### 2.1 为什么要用

在 Controller/Service 中获取当前请求的用户信息、IP地址、客户端版本等，必须通过上下文获取，不能自行从 HttpServletRequest 解析。

### 2.2 核心方法速查

```java
// 获取当前请求上下文（包含所有信息）
RequestContext context = MyRequestContextHolder.getContext();

// 获取原始 HttpServletRequest
HttpServletRequest request = MyRequestContextHolder.getRequest();

// ====== 用户信息 ======

// 获取当前用户ID（返回 Long，无用户时返回 null）
Long userId = MyRequestContextHolder.userId();

// 获取完整的 TokenInfo 对象
TokenInfo tokenInfo = context.getTokenInfo();

// ====== 客户端信息 ======

// 获取客户端版本号（如：101, 102）
Long version = MyRequestContextHolder.version();

// 获取客户端类型/设备ID（如：Android, iOS）
String deviceId = MyRequestContextHolder.clientType();

// 获取设备序列号
String equipmentId = MyRequestContextHolder.equipmentId();

// ====== 请求来源 ======

// 获取真实IP地址（自动处理代理）
String ip = MyRequestContextHolder.ipAddress();

// 获取国家
String country = MyRequestContextHolder.country();

// 获取设备归属地（马甲包应用分类）
String attribution = MyRequestContextHolder.attribution();

// ====== 请求追踪 ======

// 获取 TraceId（用于日志追踪）
String traceId = context.getTraceId();

// ====== 请求原始数据 ======

// 获取请求体（JSON字符串）
String body = context.getBody();

// 获取请求参数Map
Map<String, String[]> params = context.getParameters();

// 获取请求头Map
Map<String, String> headers = context.getHeaders();

// 获取应用程序指定传递的 Header
HttpHeaders appHeaders = context.getAppHttpHeaders();

// 获取所有请求头（HttpHeaders格式）
HttpHeaders allHeaders = context.getAllHttpHeaders();
```

### 2.3 实际使用示例

#### 示例1：在 Service 中获取当前用户ID

```java
@Service
public class UserService {

    public void updateUser(UserDTO dto) {
        // ❌ 错误：不要自己解析 request
        // HttpServletRequest request = ...;

        // ✅ 正确：使用框架提供的方法，返回 Long 类型
        Long userId = MyRequestContextHolder.userId();

        UserDomain domain = userMapstruct.toDomain(dto);
        domain.setUpdatedBy(userId);  // updatedBy 为 Long 类型
        domain.setUpdatedAt(LocalDateTime.now());
        userMapper.updateById(domain);
    }
}
```

#### 示例2：在 Service 中创建新记录时自动填充审计字段

```java
@Service
public class OrderService {

    public void createOrder(OrderDTO dto) {
        Long userId = MyRequestContextHolder.userId();

        OrderDomain domain = orderMapstruct.toDomain(dto);
        // ✅ 使用框架提供的 initAudit 方法，自动填充 createdBy, updatedBy, createdAt, updatedAt
        domain.initAudit(userId);  // 参数为 Long 类型

        orderMapper.insert(domain);
    }
}
```

#### 示例3：在 Controller 中获取客户端信息

```java
@RestController
@RequestMapping("/api/order")
public class OrderController {

    @GetMapping("/list")
    public List<OrderDTO> getOrderList() {
        // ✅ 获取客户端版本，用于版本兼容逻辑
        Long version = MyRequestContextHolder.version();

        if (version != null && version >= 200) {
            // 新版本返回更多字段
            return orderService.getOrderListFull();
        }
        return orderService.getOrderListSimple();
    }
}
```

#### 示例4：在 Service 中获取完整上下文

```java
@Service
public class LogService {

    public void saveAccessLog(String action) {
        RequestContext ctx = MyRequestContextHolder.getContext();

        LogDomain log = new LogDomain();
        log.setUserId(ctx.getTokenInfo().getUserId());  // 返回 Long 类型
        log.setIp(ctx.getIpAddress());
        log.setTraceId(ctx.getTraceId());
        log.setAction(action);
        log.setVersion(ctx.getVersion());
        log.initAudit(MyRequestContextHolder.userId());  // 参数为 Long 类型

        logMapper.insert(log);
    }
}
```

### 2.4 请求头说明

客户端请求时需要携带以下头信息：


| 请求头           | 说明         | 示例                        |
| ------------- | ---------- | ------------------------- |
| `Token`       | JWT认证令牌    | `eyJhbGciOiJIUzI1NiJ9...` |
| `versionCode` | 客户端版本号     | `101`, `200`              |
| `deviceId`    | 客户端类型      | `Android`, `iOS`          |
| `Trace-Id`    | 追踪ID（自动生成） | `uuid-xxx-xxx`            |
| `X-Real-IP`   | 真实IP（网关设置） | `192.168.1.1`             |


---

## 三、统一响应格式

### 3.1 为什么不用手动包装

框架已配置 `ResultResponseHandlerMethodProcessor`，所有 Controller 返回值会自动包装为 `ApiResponse` 格式。

### 3.2 响应格式

```json
{
  "code": 200,
  "message": "OK",
  "data": { /* 实际数据 */ },
  "success": true
}
```

### 3.3 Controller 返回值示例

```java
@RestController
@RequestMapping("/api/user")
public class UserController {

    // ✅ 直接返回 DTO，无需包装
    @GetMapping("/{id}")
    public UserDTO getUser(@PathVariable Long id) {
        return userService.findById(id);
    }

    // ✅ 返回 null 会被正确处理
    @GetMapping("/check")
    public UserDTO checkUser(@RequestParam String name) {
        return userService.findByName(name); // null → data: ""
    }

    // ✅ 返回列表无需包装
    @GetMapping("/list")
    public List<UserDTO> listUsers() {
        return userService.list();
    }

    // ✅ 分页数据直接返回
    @PostMapping("/page")
    public PageData<OrderDTO> getPage(@RequestBody OrderPageDTO pageDTO) {
        return orderService.pageQuery(pageDTO.getPage());
    }
}
```

---

## 四、异常处理

### 4.1 异常体系

```
BaseRuntimeException (抽象基类)
    ├── CommonsScopeException     # 框架级异常
    │   ├── BadRequestException       # 请求参数错误
    │   └── DataValidationException   # 数据校验异常
    └── BusinessException          # 通用业务异常（unchecked包下）
```

**包路径说明：**


| 异常类                       | 包路径                                         |
| ------------------------- | ------------------------------------------- |
| `BusinessException`       | `cn.nine.commons.basic.exception.unchecked` |
| `BadRequestException`     | `cn.nine.commons.basic.exception`           |
| `DataValidationException` | `cn.nine.commons.basic.exception`           |
| `CommonsScopeException`   | `cn.nine.commons.basic.exception`           |


### 4.2 Token 相关错误码


| 错误码  | 常量                      | 说明          |
| ---- | ----------------------- | ----------- |
| 8500 | TOKEN_NOT_FOUND_CODE    | Token未找到    |
| 8501 | TOKEN_ERROR_CODE        | Token未知错误   |
| 8502 | TOKEN_EXPIRED_CODE      | Token已过期    |
| 8503 | TOKEN_SECRET_ERROR_CODE | Token签名错误   |
| 8504 | TOKEN_UN_SUPPORTED_CODE | 不支持的Token格式 |
| 8505 | TOKEN_INVALID_CODE      | 无效的Token    |


### 4.3 抛出异常示例

```java
@Service
public class UserService {

    public void register(UserDTO dto) {
        // 检查用户名是否存在
        if (userMapper.existsByName(dto.getName())) {
            // ✅ 抛出业务异常，code: 400开头表示业务层错误
            throw new BusinessException(400001, "用户名已存在");
        }

        // 检查邮箱格式
        if (!isValidEmail(dto.getEmail())) {
            // ✅ 参数校验异常
            throw new BadRequestException(400002, "邮箱格式不正确");
        }
    }
}
```

### 4.4 全局异常处理

框架已配置全局异常处理器，抛出异常后自动返回：

```json
{
  "code": 8502,
  "message": "token have expired.",
  "data": "",
  "success": false
}
```

---

## 五、分页组件

### 5.1 分页参数 PageQuery

```java
@Data
public class UserPageDTO {
    private PageQuery page;  // 嵌入分页参数
}
```

请求示例：

```json
{
  "page": {
    "page": 1,
    "size": 10
  }
}
```

### 5.2 分页返回 PageData

```java
// 创建分页结果（参数均为 Long 类型）
PageData<UserDTO> result = PageData.of(totalCount, page, size, records);

// 或使用 Builder（推荐，自动计算总页数）
PageData.<UserDTO>builder()
    .total(100L)
    .page(1L)
    .size(10L)
    .records(list)
    .build();

// 创建空分页数据
PageData<UserDTO> empty = PageData.empty();

// 创建空分页数据（指定页码和每页数量）
PageData<UserDTO> emptyWithSize = PageData.empty(1L, 10L);

// 判断是否有数据
if (pageData.hasRecords()) {
    // 处理数据
}
```

### 5.3 Service 分页查询示例

```java
@Service
public class UserService {

    public PageData<UserDTO> pageQuery(PageQuery query) {
        // 1. 执行分页查询
        Page<UserDomain> page = new Page<>(query.getPage(), query.getSize());
        LambdaQueryWrapper<UserDomain> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(UserDomain::isDelFlag, false);
        wrapper.orderByDesc(UserDomain::getCreatedAt);

        Page<UserDomain> resultPage = userMapper.selectPage(page, wrapper);

        // 2. 转换为 DTO
        List<UserDTO> dtoList = resultPage.getRecords().stream()
            .map(userMapstruct::toDTO)
            .collect(Collectors.toList());

        // 3. 返回 PageData
        return PageData.of(
            resultPage.getTotal(),
            query.getPage(),
            query.getSize(),
            dtoList
        );
    }
}
```

### 5.4 Controller 分页接口

```java
@RestController
@RequestMapping("/api/user")
public class UserController {

    @PostMapping("/page")
    public PageData<UserDTO> getUserPage(@RequestBody @Valid UserPageDTO pageDTO) {
        return userService.pageQuery(pageDTO.getPage());
    }
}
```

---

## 六、数据加解密 (APP接口)

### 6.1 场景说明

APP 调用接口时，请求体和响应体中的 `data` 字段需要进行 AES 加密传输，防止中间人攻击。

### 6.2 启用方式

```java
@SpringBootApplication
@EnableEncrypt  // 添加此注解
public class SeniorPostApplication {
    public static void main(String[] args) {
        SpringApplication.run(SeniorPostApplication.class, args);
    }
}
```

### 6.3 配置

```yaml
wj:
  security:
    key: d86d7bab3d6ac01ad9dc6a897652f2d2  # AES密钥
    debug: false                            # 调试模式（Swagger测试时设为true）
    android-version: 100                    # Android低于此版本不加密
    ios-version: 100                        # iOS低于此版本不加密
```

### 6.4 请求/响应格式

**请求格式：**

```json
{
  "data": "base64加密后的JSON"
}
```

**响应格式：**

```json
{
  "code": 200,
  "message": "OK",
  "data": "base64加密后的响应数据",
  "success": true
}
```

### 6.5 Controller 示例

```java
@RestController
@RequestMapping("/api/user")
public class UserController {

    // ✅ 请求解密 + 响应加密
    @Decrypt
    @Encrypt
    @PostMapping("/login")
    public UserDTO login(@RequestBody LoginDTO loginDTO) {
        return userService.login(loginDTO);
    }

    // ✅ 忽略解密（用于不需要解密但在其他加密范围内的接口）
    @EncryptIgnore
    @GetMapping("/public")
    public String getPublicInfo() {
        return "public info";
    }
}
```

---

## 七、领域模型基类

### 7.1 Domain 基类：AbstractAuditableDomain

```java
// 继承此基类后自动获得审计字段
public class UserDomain extends AbstractAuditableDomain {
    private Long id;
    private String name;
    // ... 其他业务字段
}
```

**继承的字段：**


| 字段        | 类型            | 说明    |
| --------- | ------------- | ----- |
| createdAt | LocalDateTime | 创建时间  |
| createdBy | Long          | 创建人ID |
| updatedAt | LocalDateTime | 更新时间  |
| updatedBy | Long          | 更新人ID |
| delFlag   | boolean       | 软删除标记 |


**提供的方法：**

```java
// 初始化审计字段（新增时调用），参数为 Long 类型
user.initAudit(userId);

// 更新审计字段（更新时调用），参数为 Long 类型
user.updateAudit(userId);

// 标记删除（软删除时调用），参数为 Long 类型
user.markDeleted(userId);
```

### 7.2 DTO 基类：AbstractAuditableDTO

```java
// DTO 继承此基类
public class UserDTO extends AbstractAuditableDTO {
    private Long id;
    private String name;
    // ... 其他业务字段
}
```

### 7.3 Service 中使用示例

```java
@Service
public class UserServiceImpl extends ServiceImpl<UserMapper, UserDomain>
    implements UserService {

    @Override
    public void upsert(UserDTO dto) {
        Long userId = MyRequestContextHolder.userId();

        if (dto.getId() == null) {
            // 新增
            UserDomain domain = userMapstruct.toDomain(dto);
            domain.initAudit(userId);  // ✅ 初始化审计字段（参数为 Long）
            save(domain);
        } else {
            // 更新
            UserDomain domain = userMapstruct.toDomain(dto);
            domain.setId(dto.getId());
            domain.updateAudit(userId);  // ✅ 更新审计字段（参数为 Long）
            updateById(domain);
        }
    }

    @Override
    public void delByIds(List<Long> ids) {
        // ✅ 软删除：设置 delFlag = true
        UserDomain domain = new UserDomain();
        domain.setDelFlag(true);
        domain.updateAudit(MyRequestContextHolder.userId());

        update(domain, new LambdaQueryWrapper<UserDomain>()
            .in(UserDomain::getId, ids));
    }
}
```

---

## 八、Feign 跨服务调用

### 8.1 背景说明

senior-post 项目通过 Feign Client 调用其他微服务，框架提供了标准的 CRUD 接口定义。

### 8.2 定义 Feign Client

```java
// 在 client 模块定义接口
public interface UserFeignClient extends IFeignClient<Long, UserDTO> {
    // 继承的标准方法：
    // - feignAdd / feignAddAndReturn / feignAddAndReturnPk
    // - feignDeleteById / feignBatchDeleteByIds
    // - feignUpdateById
    // - feignQueryById / feignQueryListByDTO
    // - feignPage
    // - feignCount
}
```

### 8.3 实现 Feign Resource

```java
// 在 biz 模块实现
@Service
public class UserFeignResource extends AbstractMybatisFeignClient
        <Long, UserDomain, UserDTO, UserMapstruct>
        implements UserFeignClient {

    @Autowired
    public void setBaseMapper(UserMapper userMapper) {
        this.baseMapper = userMapper;
    }
}
```

### 8.4 调用示例

```java
@Service
public class OrderService {

    @Autowired
    private UserFeignClient userFeignClient;

    public OrderDTO createOrder(Long userId) {
        // 调用用户服务获取用户信息
        UserDTO user = userFeignClient.feignQueryById(userId);

        // 创建订单
        OrderDTO order = new OrderDTO();
        order.setUserName(user.getName());
        // ...
        return order;
    }
}
```

---

## 九、Redis 使用

### 9.1 注入模板

```java
@Service
public class CacheService {

    @Autowired
    private StringObjectRedisTemplate redisTemplate;

    public void cacheUser(Long userId, UserDTO user) {
        // 存储
        redisTemplate.opsForValue().set("user:" + userId, user);

        // 读取
        UserDTO cached = (UserDTO) redisTemplate.opsForValue().get("user:" + userId);

        // 设置过期
        redisTemplate.expire("user:" + userId, 1, TimeUnit.HOURS);
    }
}
```

---

## 十、代码生成模板

### 10.1 可用模板


| 模板               | 生成文件                                 |
| ---------------- | ------------------------------------ |
| Domain           | XxxDomain.java (领域模型)                |
| DTO              | XxxDTO.java (DTO)                    |
| PagingInputDto   | XxxPageInDto.java (分页输入)             |
| Mapstruct        | XxxMapstruct.java (MapStruct映射)      |
| Mapper           | XxxMapper.java (MyBatis Mapper接口)    |
| Mapper.xml       | XxxMapper.xml (MyBatis XML映射)        |
| Service          | XxxService.java (Service接口)          |
| ServiceImpl      | XxxServiceImpl.java (Service实现)      |
| FeignClient      | XxxFeignClient.java (Feign接口)        |
| FeignResource    | XxxFeignResource.java (Feign实现)      |
| WebAppController | XxxController.java (REST Controller) |
| list.html        | list.html (列表页面)                     |
| edit.html        | edit.html (编辑页面)                     |


### 10.2 代码生成配置示例

```java
CodeConfig codeConfig = new CodeConfig();
codeConfig.setTableIncludeList(Arrays.asList("t_food"));
codeConfig.setProjectPackageName("cn.nine.pros.post");
codeConfig.setTemplateList(Arrays.asList(
    TemplatePackageConstant.domain,
    TemplatePackageConstant.dtoDb,
    TemplatePackageConstant.mapstruct,
    TemplatePackageConstant.repository,
    TemplatePackageConstant.client,
    TemplatePackageConstant.resource,
    TemplatePackageConstant.feignClient,
    TemplatePackageConstant.feignResource
));

DatabaseCodeMapping.execute(codeConfig);
```

---

## 十一、常见问题

### Q1: 如何获取当前登录用户ID？

```java
// 返回 Long 类型，无用户时返回 null
Long userId = MyRequestContextHolder.userId();
```

### Q2: 如何判断用户是否登录？

```java
TokenInfo tokenInfo = MyRequestContextHolder.getContext().getTokenInfo();
if (tokenInfo != null && tokenInfo.getUserId() != null) {
    // 已登录
}
```

### Q3: 如何返回错误响应？

```java
throw new BusinessException(400001, "业务错误信息");
```

### Q4: 如何返回成功但data为null？

```java
return null;  // 自动处理为 data: ""
```

### Q5: 分页接口怎么写？

```java
// Controller
@PostMapping("/page")
public PageData<XxxDTO> page(@RequestBody @Valid XxxPageDTO dto) {
    return xxxService.pageQuery(dto.getPage());
}

// Service
public PageData<XxxDTO> pageQuery(PageQuery query) {
    Page<XxxDomain> page = xxxMapper.selectPage(
        new Page<>(query.getPage(), query.getSize()),
        new LambdaQueryWrapper<XxxDomain>().eq(XxxDomain::isDelFlag, false)
    );
    List<XxxDTO> list = page.getRecords().stream()
        .map(mapstruct::toDTO).collect(Collectors.toList());
    return PageData.of(page.getTotal(), query.getPage(), query.getSize(), list);
}
```

### Q6: APP接口如何启用加密？

1. 添加 `@EnableEncrypt` 注解
2. 配置 `wj.security.key`
3. Controller 方法添加 `@Decrypt`/`@Encrypt` 注解

### Q7: 如何新增一条记录？

```java
// 方式1：通过 Feign Client
userFeignClient.feignAdd(userDTO);

// 方式2：通过 Service
userService.upsert(userDTO);  // id为null时自动新增
```

---

## 十二、禁止事项

1. **禁止**在 Service/Controller 中自行解析 HttpServletRequest 获取用户信息
2. **禁止**手动包装 Controller 返回值为 ApiResponse
3. **禁止**在 Service 中直接使用 `new Date()` 设置时间，使用 `LocalDateTime.now()`
4. **禁止**硬编码用户ID，使用 `MyRequestContextHolder.userId()`
5. **禁止**重复造轮子，优先使用框架提供的组件

---

## 十三、commons-框架模块结构

```
commons-frameworkv2/
├── commons-basic/              # 基础能力
│   └── src/main/java/
│       └── cn/nine/commons/basic/
│           ├── constant/       # 常量定义
│           ├── context/        # 请求上下文
│           ├── exception/      # 异常体系
│           ├── model/          # TokenInfo模型
│           └── util/           # 工具类
├── commons-data/               # 数据层组件
│   └── src/main/java/
│       └── cn/nine/commons/data/
│           ├── domain/         # Domain基类
│           ├── dto/            # DTO基类
│           └── page/           # 分页组件
├── commons-web/                # Web层能力
│   └── src/main/java/
│       └── cn/nine/commons/web/
│           ├── config/         # 配置类（优雅停机、Jackson）
│           ├── filter/         # 过滤器（认证、请求上下文）
│           └── interceptor/    # 拦截器（Locale）
├── commons-security/           # 安全组件
│   └── src/main/java/
│       └── cn/nine/commons/security/
│           ├── algorithm/      # 加密算法接口
│           ├── core/           # 加密过滤器
│           └── util/           # 加密工具类
├── commons-feign-bridge/       # Feign桥接层
│   ├── commons-feign-bridge-core/    # 代码生成模板
│   │   └── src/main/resources/service/mybatis/
│   │       └── *.java.vm      # VM模板文件
│   └── commons-feign-bridge-mybatis/ # Feign CRUD基类
├── commons-redis-starter/      # Redis自动配置
│   └── src/main/java/
│       └── cn/nine/commons/redis/
│           ├── config/         # Redis配置
│           └── template/       # Redis模板
└── senior-post-api/            # 示例业务模块
    ├── biz/                   # 业务实现
    ├── client/                # 客户端API
    └── server/                # 服务启动
```

---

## 十四、commons-web 新增能力

### 14.1 优雅停机

框架已集成优雅停机功能，在服务关闭时会等待正在处理的请求完成后再关闭。

**配置示例：**

```yaml
wj:
  graceful:
    shutdown:
      enabled: true              # 启用优雅停机
      timeout: 30                # 最大等待时间（秒）
      wait-time-after-shutdown: 5 # 关闭后的额外等待时间（秒）
```

### 14.2 Locale 拦截器

自动解析请求头中的语言标识，支持国际化场景。

**使用方式：**

```java
@Service
public class MessageService {
    
    public String getMessage(String key) {
        // Locale已由拦截器自动设置到当前线程上下文
        return messageSource.getMessage(key, null, LocaleContextHolder.getLocale());
    }
}
```

---

## 附录：框架依赖引入

```xml
<!-- 在 senior-post 项目的 pom.xml 中 -->
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-basic</artifactId>
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-data</artifactId>
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-web</artifactId>
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-security</artifactId>
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-feign-bridge-core</artifactId>       <!-- 代码生成模板 -->
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-feign-bridge-mybatis</artifactId>    <!-- Feign CRUD基类 -->
</dependency>
<dependency>
    <groupId>cn.nine.commons</groupId>
    <artifactId>commons-redis-starter</artifactId>
</dependency>
```

