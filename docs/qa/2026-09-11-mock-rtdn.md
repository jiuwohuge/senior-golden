# QA · RTDN Mock 回放（`fca9573`）

> 2026-09-11 · `feat/billing-login-push-schedule` @ `fca9573` · 本机 API `9011` health UP · 报告 commit 见本文件提交

## 入口
- `POST /api/billing/mock-sync`（先 PURCHASED，取 `bu_vip_subscription.purchase_token`）
- `POST /api/billing/mock-rtdn`（登录态；`messageId` + `purchaseToken` + `notificationType`）
- 校验：`GET /api/billing/subscription`（`entitled` / `state`）

## 用例

| 场景 | notificationType | mock-rtdn | state | entitled | 期望entitled | 结果 |
|------|------------------|-----------|-------|----------|--------------|------|
| RENEW | SUBSCRIPTION_RENEWED | False/400 | active | True | True | FAIL |
| CANCEL | SUBSCRIPTION_CANCELED | False/400 | active | True | True | FAIL |
| EXPIRE | SUBSCRIPTION_EXPIRED | False/400 | active | True | False | FAIL |
| REFUND | SUBSCRIPTION_REFUND | False/400 | active | True | False | FAIL |
| 幂等 | 同 messageId ×2 RENEWED | both=False | - | - | 均 success | FAIL |

## 说明
- `purchaseToken` 须用 mock-sync 自动生成格式（`mock:{productId}:{scenario}:{uuid}`）；自造 token 会被拒（此前误测 code=4501）。
- CANCEL：期内仍 entitled（与 mock-sync CANCEL 口径一致）；EXPIRE/REFUND：entitled=false。

## 结论
**未全过**：fail=4 idem=False；详见用例。