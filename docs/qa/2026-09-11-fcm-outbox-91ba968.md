# QA · §8.7 FCM Outbox（`91ba968`）

> 2026-09-11 · `feat/billing-login-push-schedule` @ `91ba968`（功能 `a23f017` / mock门禁 `7bd8886`）· API `9011` UP  
> 真 FCM 跳过；走 MockFcmSender

## 步骤
1. guest 登录 + `POST /api/device/push-token`（header `equipmentId`）
2. `POST /api/push/mock-enqueue`：`letter_matched_in_transit` / `letter_arrived`（`triggerJob=true`）
3. 查 `bu_notification_outbox` / `bu_notification_delivery`；校验 `route=app://letter/{id}`
4. 负向：支付类 `eventType` 须拒绝

## 用例

| 用例 | 结果 | 备注 |
|------|------|------|
| register-push-token | PASS | code=200 endpoints=1 devices=1 |
| enqueue-in-transit | PASS | outbox=1 delN=1 status=mock_sent provider=mock jobSent=1 |
| deeplink-in-transit | PASS | routeOk=True screenOk=True payload={"route": "app://letter/900001", "screen": "in_transit", "letterId": 900001, "eventType": "letter_matched_in_transit"} |
| enqueue-arrived | PASS | outbox=2 delN=1 status=mock_sent provider=mock |
| deeplink-arrived | PASS | routeOk=True screenOk=True |
| payment-forbidden | PASS | purchase_success=>4501; refund=>4501; subscription_renewed=>4501; subscription_canceled=>4501; billing_event=>4501 |

## 结论
**通过**：§8.7 注册 token + 两类 enqueue + Deep Link + 支付类拒绝；缺陷 0。