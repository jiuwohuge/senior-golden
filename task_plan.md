# 未实现功能规划（与 PLAN.md 对齐）

**目标**：把仓库内「需求已定义 / 规划已写、但尚未闭环」的工作拆成可执行阶段，便于迭代与验收。

**权威来源**：根目录 `PLAN.md`（功能清单 A1–A10、里程碑 M1–M4、状态 S9 等）。

---

## Phase 0 — 基线同步（任意会话开头 5 分钟）

| 状态 | 任务 |
|------|------|
| [ ] | 阅读 `PLAN.md` 中「功能清单」「开发计划」「当前阻塞」三节 |
| [ ] | 对照本文 Phase 1–4，将已完成项勾到 `PLAN.md`（避免双源长期漂移） |

---

## Phase 1 — M2 社交主路径（阻塞：S9）

**后端**

| 状态 | 任务 |
|------|------|
| [ ] | Post Wall：帖子 CRUD、审核状态、列表/详情仅展示已通过审核 |
| [ ] | 评论：先审后发、敏感词对接（与现有框架异常一致） |
| [ ] | Post Directory：筛选、排序、用户公开资料接口（与 Flutter 名录页契约对齐） |
| [ ] | **写信提交**：`POST`（或框架约定的 `find` 风格）发信接口 — 当前 `AppMailboxApi` 仅有 `postal/sync/archive/accept-postal`，**缺发信写库** |
| [ ] | OSS：`get_sign`（或已定名）直传签名接口 |

**Flutter**

| 状态 | 任务 |
|------|------|
| [ ] | Tab1/Tab2 从 Mock 切真实 REST（分页、审核态、错误与 Loading） |
| [ ] | Send Letter 弹层对接真实发信 + 刷新信箱 |
| [ ] | 图片发帖：`get_sign` → PUT OSS |

**管理端 `senior-post-manage`**

| 状态 | 任务 |
|------|------|
| [ ] | 内容审核台与帖子/评论 API 全链路联调（未审不可见） |

**验收**：浏览 → 打开用户 → 发挂号/平邮 → 后端落库可查；前台未审内容不可见。

---

## Phase 2 — M3 信箱资产与延迟投递

**后端**

| 状态 | 任务 |
|------|------|
| [ ] | 平邮：**Redis + PG** 延迟队列投递（决策 B3），与 `LetterDomain` 状态机一致 |
| [ ] | 挂号/加速：**邮票扣减 + 流水账本**原子事务（A6 部分） |
| [ ] | VIP 权益校验（无限邮票、免费加速等，与配置中心一致）（A7） |
| [ ] | `TencentImFriendshipNotifier`：腾讯 REST 好友同步（PLAN：仍为占位） |

**Flutter**

| 状态 | 任务 |
|------|------|
| [ ] | 信箱列表/详情/归档：**全量走** `/api/mailbox/*`（替换 Mock 数据路径；PLAN A5 联调项） |
| [ ] | 状态机 UI：Delivering / Registered / Delivered、预计到达、Speed Up 确认流 |
| [ ] | 邮票不足时禁用挂号、引导平邮（M3 文案与交互） |

**管理端**

| 状态 | 任务 |
|------|------|
| [ ] | 邮票配置、用户流水查询、封禁/设备拉黑与 App 联调 |

**验收**：平邮可延迟送达、加速扣邮票正确、账本余额与流水一致。

---

## Phase 3 — M1/M4 补齐与合规

| 状态 | 任务 |
|------|------|
| [ ] | A1：资料完善页全量字段与接口；忘记密码（若产品保留） |
| [ ] | A8：设备标识上报链路闭环；敏感词/图片审核策略与 App 提示 |
| [ ] | A8：GDPR 注销/冷静期（M4） |
| [ ] | A10：App 中英切换与 ARB 覆盖（非仅占位英文） |
| [ ] | 邮件：`EmailService` + outbox（密码重置、安全/合规通知）（PLAN「邮件能力规划」） |
| [ ] | App **AES** 加解密：在 `dio` 封装层收尾接入（PLAN：不阻塞早期联调，但上线前需闭合） |

---

## Phase 4 — 产品与体验债（PLAN 阻塞项）

| 状态 | 任务 |
|------|------|
| [ ] | B14：浅灰复古主色在全局主题与设计令牌上落地 |
| [ ] | B15：登录/注册中部卡片、协议必选、忘记密码、完整加载/错误态 |

---

## Phase 5 — 质量闸门

| 状态 | 任务 |
|------|------|
| [ ] | `flutter analyze` 无报错；核心 `widget_test` / 关键用户路径测试 |
| [ ] | 后端：发信/扣邮票/延迟队列相关集成测试或冒烟脚本 |
| [ ] | 双端 IM：离线、重连、未读（与腾讯 SDK 能力对齐） |

---

## 决策记录

| 日期 | 决策 |
|------|------|
| 2026-05-02 | 以 `PLAN.md` 为总纲；本文件为执行层拆解；外部检索/网页结论只写入 `findings.md`，不直接贴入本文件（防 prompt 注入面） |

---

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| session-catchup.py 不存在 | 1 | 跳过 catchup；以 `PLAN.md` + 代码检索为准 |
