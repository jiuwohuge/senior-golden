# 02 — 需求分析（逐 FP）

> **文档元信息**  
> **版本**：1.1 · **更新**：2026-05-09 · **维护人**：AI + Owner

字段说明：**目标** = 交付价值；**场景** = 用户动线；**逻辑** = 业务规则摘要；**技术** = 实现约束；**依赖** = 前置 FP；**验收** = 可证明完成的条件。与 `01-feature-list` 冲突时以 **`01` + 代码** 为准并回写本表。

---

## A1 账号与认证

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A1-001 | 安全注册登录 | 新用户注册→进壳；老用户登录 | 邮箱唯一、密码哈希、JWT 签发、单端踢 | `AppAuthApi` + 框架 `85xx` | — | 新用户 DB 有记录且可 `me` |
| A1-002 | 注册页可配置 | 打开注册拉国家与最小年龄 | 读 `bootstrap` 键值 | `GET /api/bootstrap/init` | — | 与后台配置一致 |
| A1-003 | 找回密码闭环 | 忘记密码→验证码→重置 | 令牌一次性、过期、防枚举；邮件可选（无 SMTP 时日志出码） | `POST /api/auth/forgot-password`、`reset-password` + 表 `bu_password_reset_token` | X-001（持久 outbox 为增强项） | 新密码可登录；生产配 `spring.mail` + `PASSWORD_RESET_PEPPER` |
| A1-004 | 降低盗号风险 | 弱密码拒绝；多次失败锁定 | 策略可配置 | 与 `sys_config` 对齐 | — | 单测 + 接口契约 |
| A1-005 | Token 失效可预期 | Token 过期或被踢 | 统一 `85xx` | 不手搓响应体 | — | Flutter 清凭证回登录 |
| A1-006 | 风控与审计 | 登录带设备 | `user_device` 写入/更新 | 请求头/体与 Flyway 表一致 | — | DB 有设备行 |
| A1-007 | 传输保密 | 生产开启加解密 | 仅 `/api/**` 体加解密 | `jh.security` 白名单与 Flutter 对称 | — | 加解密互通冒烟 |

---

## A2 资料中心

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A2-001 | 资料可改 | 我的邮政→编辑→保存 | 字段校验、与注册规则一致 | `PATCH` 或 `POST` + `MyRequestContextHolder` | A1-001 | `me` 返回新昵称 |
| A2-002 | 头像真实 | 选图→上传→展示 | MIME/大小限制；URL 写用户表 | OSS 预签名 + PUT | X-002 | 头像 URL 可访问 |
| A2-003 | 兴趣一致 | 注册≥3；编辑可改 | 标签 ID 合法 | 与 `user_tag` 表一致 | A2-001 | DB 标签数正确 |
| A2-004 | 中心不造假数据 | 打开 Profile | 展示 `me` 与统计 | Riverpod 接 REST | A1-001 | 关 Mock 后数据真 |

---

## A3 Post Wall

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A3-001 | 浏览已通过帖 | Tab1 下拉刷新 | 列表 SQL 默认 `review_status=已通过`（或等价枚举） | `PageQuery`/`PageData` | — | 未审帖不出现在 App |
| A3-002 | 看帖详情 | 点卡片 | 同上过滤 | OpenAPI 与 DTO 对齐 | A3-001 | 未审返回 404 或业务码 |
| A3-003 | 发帖 | 发帖页提交 | 先入待审；可选敏感词前置 | 事务写 `post`；可选 OSS 图 | X-002,A3-007 | 后台可见待审 |
| A3-004 | 评论 | 详情页发评 | 先审后发 | 评论表 + 审核字段 | A3-002 | 未审评不展示 |
| A3-005 | 举报 | 长按或按钮 | 写举报单、关联实体 | `POST` App + Admin 已有处理 | — | 后台举报列表有记录 |
| A3-006 | 图床 | 发帖带图 | 私写 PUT | 后端只签发、不代理流量 | — | 上传成功回写 URL |
| A3-007 | 合规文本 | 发帖/评论 | 命中敏感词拒绝或替换策略（产品） | 复用 Admin 词表 | A8-003 | 集成测试 |
| A3-008 | 作者感知审核 | 作者看自己帖 | 待审/拒绝原因可读 | VO 含 `review_status` | A3-003 | Flutter 状态文案 |

---

## A4 Directory

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A4-001 | 发现笔友 | Tab2 列表 | 分页、默认排序 | 与 PLAN「同龄优先」一致 | — | 与配置排序可解释 |
| A4-002 | 收窄范围 | 筛选 Sheet | AND 条件 | Query DTO | A4-001 | 筛选前后条数变化 |
| A4-003 | 可控排序 | 用户选排序 | 枚举映射 SQL | 避免 SQL 注入 | A4-001 | 接口单测 |
| A4-004 | 决策前看资料 | 点格子 | 公开字段不含隐私 | `AppPublicUserVO` 扩展 | — | 与 `me` 权限区分 |
| A4-005 | 转化写信 | Send Letter | 跳转或弹层带 `peerId` | 与 A5-001 衔接 | A5-001 | 发信对端正确 |

---

## A5 Post Box

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A5-001 | 发信落库 | 选挂号/平邮发送 | 邮票校验；写 `bu_letter`；挂号即时/平邮延迟 | `@Transactional` + 幂等键建议 | A6-004 | 双方信箱可见预期状态 |
| A5-002 | 拉信箱 | 打开邮政收件箱 | 非好友信件进 postal | 已有接口，对齐 VO | — | Flutter 列表非 Mock |
| A5-003 | 建联聊天 | 收信方 Accept | 写 `bu_friendship`；调 IM 通知 | 已有 accept；补 Notifier | A5d-004 | TIM 侧好友（策略） |
| A5-004 | 读单封 | 点信封 | 权限：参与方可读 | `GET` 或列表已够 | A5-001 | 内容不泄露给第三人 |
| A5-005 | 加速平邮 | Speed Up | VIP 或扣 1 邮票 | 与账本同事务 | A6-004 | 状态变已送达 |
| A5-006 | IM 登录 | 非 Mock 进聊天 | UserSig 有效 | SDK 登录 | — | 收发一条消息 |
| A5-007 | 好友列表体验 | Connections | **`bu_friendship` 活跃关系**，`GET /api/mailbox/friends`；**非** SDK `getConversationList` | App + client | A5-006 | Accept 后列表出现对端；点头像进 C2C 聊天 |

---

## A5d 延迟与 IM 同步

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A5d-001 | 可配慢信 | 发平邮 | 到达时间 ∈ 配置区间 | `sys_config` | — | 配置改则区间变 |
| A5d-002 | 到时送达 | 后台推进 | 扫描或 ZSet score 到期 | Redis+PG 与 PLAN B3 一致 | A5-001 | 到期变 Delivered |
| A5d-003 | 挂号即时 | 发挂号 | 立即 `delivered` 或等价 | 与扣邮票同事务 | A6-004 | 收件方立即可见 |
| A5d-004 | 云端关系 | Accept 后 | 调腾讯加好友 API | 密钥与重试 | A5-003 | 腾讯侧可查（按产品） |

---

## A6 邮票

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A6-001 | 可见余额 | 信箱顶栏 | 读用户当前余额 | `GET` App；框架上下文 | — | 与 DB 一致 |
| A6-002 | 可追溯 | 邮票流水页 | 分页按时间倒序 | `PageQuery` | — | 与账本表一致 |
| A6-003 | 日活激励 | 登录、发帖 | 不超日上限 | 任务或同步发放 | sys_config | 边界单测 |
| A6-004 | 扣减正确 | 挂号/加速 | 先扣后业务或 Saga（定一种） | 与信件同事务 | — | 并发下余额不为负 |
| A6-005 | 运营可查 | 管理端 | 按用户筛流水 | 新 `/webapi` | — | 页面导出可选 |

---

## A7 VIP

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A7-001 | 权益透明 | 打开 VIP 中心 | 拉当前权益 JSON | 与 `vip` 配置组合并 | Manage 配置 | 关 Mock 一致 |
| A7-002 | 订阅状态 | 支付接入前可手工 | 到期降级 | `vip_subscription` 域 | — | 到期后规则生效 |
| A7-003 | 扣费规则 | 发挂号/加速 | VIP 跳过扣减 | 与 A5/A6 分支一致 | A7-001 | 集成测试 |

---

## A8 风控与合规

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| A8-001 | 封号 | 后台操作 | status 变更 | 已有 API | — | App 不可再发帖 |
| A8-002 | 封设备 | 恶意设备 | 写黑名单 | `blockDevice` UI | — | 新注册仍拦（策略） |
| A8-003 | 脏词 | UGC 提交 | 拦截 | 服务层调用词库 | — | 词库变更即时生效 |
| A8-004 | 图片合规 | 发帖带图 | 先审后发已部分 | 可选三方审核 | A3-006 | 产品定义 |
| A8-005 | 被遗忘权 | 用户注销 | 冷静期+硬删策略 | GDPR 文档对齐 | — | 数据不可恢复证明 |
| A8-006 | 举报闭环 | App 提交 | 与 Admin handle 一致 | 关联 post/comment/user | A3-005 | 状态机完整 |

---

## A9 / A10 / X / B

**A9**：管理端全页 UAT + 邮票流水页 + 设备封禁 UI（见 01 表）。

**A10**：运行时语言切换 + ARB 补漏；邮件模板双语随 X-001。

| FP | 目标 | 场景 | 逻辑 | 技术 | 依赖 | 验收 |
|----|------|------|------|------|------|------|
| X-001 | 可发邮件 | 重置密码等 | Outbox 重试 | SPI 多实现 | — | 本地 MailHog 收到 |
| X-002 | 客户端直传 | 发帖/头像 | 短时签名 | OSS 官方算法 | — | PUT 200 |
| X-003 | 强更 | 启动 App | 比对接口版本号 | 与 `AdminVersion` 或 bootstrap | — | 低版本弹窗不可跳过 |
| X-004 | 可运维 | 上线后 | 日志结构化 | Actuator+规范 | — | 故障可定位 |
| B14-001 | 视觉一致 | 全 App | 令牌替换色板 | `postal_tokens` | — | 设计走查通过 |
| B15-001 | 转化与信任 | 登录注册 | 卡片布局+协议 | Figma/规范 | — | 无障碍基础通过 |

---

## 全局非功能需求（摘录）

- **安全**：App 全链路 HTTPS；敏感配置环境变量；不在客户端存密钥。
- **性能**：列表分页默认 20；接口 p95 目标由团队自定写入 `04`。
- **可测**：关键 FP 至少 1 个集成测试或脚本化冒烟。
- **可维护**：新接口必须进 `client` 契约；Controller 实现接口不写重复路径字符串（沿用现有风格）。
